type WasteContainer = {
    id: string;
    fillingLevel: number;
    location: [number, number];
    name: string;
    hasDeviceMeasurement: string;
    hasZone: string;
}

export default WasteContainer;
